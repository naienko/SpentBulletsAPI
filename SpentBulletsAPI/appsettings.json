{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
    //REEARCH NOTE -- learn how to create the connection string for MySQL instead of SQL Server
  "ConnectionStrings": {
    //TO DO: Make sure to alter this for when we go live -- find out what it should be then
    "DefaultConnection": "server=127.0.0.1;port=3311;datebase=spent_bullets;user=root;password=h1s3k117"
  }
}
